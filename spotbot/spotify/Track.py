class Track:
    def __init__(self, track_name, artist, album, uri, image_url, search_index, total_search_results):
        self.track_name = track_name
        self.artist = artist,
        self.album = album
        self.uri = uri
        self.image_url = image_url,
        self.search_index = search_index
        self.total_search_results = total_search_results
